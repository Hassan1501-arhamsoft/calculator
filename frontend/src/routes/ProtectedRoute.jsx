/**
 * Reserved for when this project adds a login system — would redirect
 * to /login if there's no authenticated user. The calculator has no
 * auth yet, so this currently just renders its children unchanged.
 * Named/placed to match the reference project's routes/ folder.
 */
export default function ProtectedRoute({ children }) {
  return children;
}
